# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class DockerContainerActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.DockerContainer]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await DockerContainer.prisma().query_raw(
            'SELECT * FROM DockerContainer WHERE id = $1',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.DockerContainer
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await DockerContainer.prisma().query_first(
            'SELECT * FROM DockerContainer WHERE containerId = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.DockerContainerCreateInput,
        include: Optional[types.DockerContainerInclude] = None
    ) -> _PrismaModelT:
        """Create a new DockerContainer record.

        Parameters
        ----------
        data
            DockerContainer record data
        include
            Specifies which relations should be loaded on the returned DockerContainer model

        Returns
        -------
        prisma.models.DockerContainer
            The created DockerContainer record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a DockerContainer record from just the required fields
        dockercontainer = await DockerContainer.prisma().create(
            data={
                # data to create a DockerContainer record
                'containerId': 'ggciceaie',
                'name': 'bbehjachib',
                'status': 'cadfabfehe',
                'image': 'dgiiaaijj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.DockerContainerCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple DockerContainer records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of DockerContainer record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await DockerContainer.prisma().create_many(
            data=[
                {
                    # data to create a DockerContainer record
                    'containerId': 'bfaiacjjfc',
                    'name': 'eigcfgbif',
                    'status': 'bagcfbhiig',
                    'image': 'cghideieh',
                },
                {
                    # data to create a DockerContainer record
                    'containerId': 'biabhbdai',
                    'name': 'idghgaicb',
                    'status': 'fjfddhigg',
                    'image': 'hjaecfifb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.DockerContainerWhereUniqueInput,
        include: Optional[types.DockerContainerInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single DockerContainer record.

        Parameters
        ----------
        where
            DockerContainer filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned DockerContainer model

        Returns
        -------
        prisma.models.DockerContainer
            The deleted DockerContainer record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dockercontainer = await DockerContainer.prisma().delete(
            where={
                'id': 2111915288,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.DockerContainerWhereUniqueInput,
        include: Optional[types.DockerContainerInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique DockerContainer record.

        Parameters
        ----------
        where
            DockerContainer filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DockerContainer model

        Returns
        -------
        prisma.models.DockerContainer
            The found DockerContainer record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dockercontainer = await DockerContainer.prisma().find_unique(
            where={
                'id': 1149758321,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.DockerContainerWhereUniqueInput,
        include: Optional[types.DockerContainerInclude] = None
    ) -> _PrismaModelT:
        """Find a unique DockerContainer record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            DockerContainer filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DockerContainer model

        Returns
        -------
        prisma.models.DockerContainer
            The found DockerContainer record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dockercontainer = await DockerContainer.prisma().find_unique_or_raise(
            where={
                'id': 1644289366,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DockerContainerWhereInput] = None,
        cursor: Optional[types.DockerContainerWhereUniqueInput] = None,
        include: Optional[types.DockerContainerInclude] = None,
        order: Optional[Union[types.DockerContainerOrderByInput, List[types.DockerContainerOrderByInput]]] = None,
        distinct: Optional[List[types.DockerContainerScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple DockerContainer records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of DockerContainer records returned
        skip
            Ignore the first N results
        where
            DockerContainer filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DockerContainer model
        order
            Order the returned DockerContainer records by any field
        distinct
            Filter DockerContainer records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.DockerContainer]
            The list of all DockerContainer records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 DockerContainer records
        dockercontainers = await DockerContainer.prisma().find_many(take=10)

        # find the first 5 DockerContainer records ordered by the name field
        dockercontainers = await DockerContainer.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DockerContainerWhereInput] = None,
        cursor: Optional[types.DockerContainerWhereUniqueInput] = None,
        include: Optional[types.DockerContainerInclude] = None,
        order: Optional[Union[types.DockerContainerOrderByInput, List[types.DockerContainerOrderByInput]]] = None,
        distinct: Optional[List[types.DockerContainerScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single DockerContainer record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DockerContainer filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DockerContainer model
        order
            Order the returned DockerContainer records by any field
        distinct
            Filter DockerContainer records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DockerContainer
            The first DockerContainer record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DockerContainer record ordered by the status field
        dockercontainer = await DockerContainer.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DockerContainerWhereInput] = None,
        cursor: Optional[types.DockerContainerWhereUniqueInput] = None,
        include: Optional[types.DockerContainerInclude] = None,
        order: Optional[Union[types.DockerContainerOrderByInput, List[types.DockerContainerOrderByInput]]] = None,
        distinct: Optional[List[types.DockerContainerScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single DockerContainer record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DockerContainer filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DockerContainer model
        order
            Order the returned DockerContainer records by any field
        distinct
            Filter DockerContainer records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DockerContainer
            The first DockerContainer record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DockerContainer record ordered by the image field
        dockercontainer = await DockerContainer.prisma().find_first_or_raise(
            skip=1,
            order={
                'image': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.DockerContainerUpdateInput,
        where: types.DockerContainerWhereUniqueInput,
        include: Optional[types.DockerContainerInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single DockerContainer record.

        Parameters
        ----------
        data
            DockerContainer record data specifying what to update
        where
            DockerContainer filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned DockerContainer model

        Returns
        -------
        prisma.models.DockerContainer
            The updated DockerContainer record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        dockercontainer = await DockerContainer.prisma().update(
            where={
                'id': 1388290519,
            },
            data={
                # data to update the DockerContainer record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.DockerContainerWhereUniqueInput,
        data: types.DockerContainerUpsertInput,
        include: Optional[types.DockerContainerInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            DockerContainer filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned DockerContainer model

        Returns
        -------
        prisma.models.DockerContainer
            The created or updated DockerContainer record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dockercontainer = await DockerContainer.prisma().upsert(
            where={
                'id': 1647418052,
            },
            data={
                'create': {
                    'id': 1647418052,
                    'containerId': 'biabhbdai',
                    'name': 'idghgaicb',
                    'status': 'fjfddhigg',
                    'image': 'hjaecfifb',
                },
                'update': {
                    'containerId': 'biabhbdai',
                    'name': 'idghgaicb',
                    'status': 'fjfddhigg',
                    'image': 'hjaecfifb',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.DockerContainerUpdateManyMutationInput,
        where: types.DockerContainerWhereInput,
    ) -> int:
        """Update multiple DockerContainer records

        Parameters
        ----------
        data
            DockerContainer data to update the selected DockerContainer records to
        where
            Filter to select the DockerContainer records to update

        Returns
        -------
        int
            The total number of DockerContainer records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all DockerContainer records
        total = await DockerContainer.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DockerContainerWhereInput] = None,
        cursor: Optional[types.DockerContainerWhereUniqueInput] = None,
    ) -> int:
        """Count the number of DockerContainer records present in the database

        Parameters
        ----------
        select
            Select the DockerContainer fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DockerContainer filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DockerContainerCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DockerContainer.prisma().count()

        # results: prisma.types.DockerContainerCountAggregateOutput
        results = await DockerContainer.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.DockerContainerCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DockerContainerWhereInput] = None,
        cursor: Optional[types.DockerContainerWhereUniqueInput] = None,
    ) -> types.DockerContainerCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.DockerContainerCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DockerContainerWhereInput] = None,
        cursor: Optional[types.DockerContainerWhereUniqueInput] = None,
    ) -> Union[int, types.DockerContainerCountAggregateOutput]:
        """Count the number of DockerContainer records present in the database

        Parameters
        ----------
        select
            Select the DockerContainer fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DockerContainer filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DockerContainerCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DockerContainer.prisma().count()

        # results: prisma.types.DockerContainerCountAggregateOutput
        results = await DockerContainer.prisma().count(
            select={
                '_all': True,
                'containerId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DockerContainerCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.DockerContainerWhereInput] = None
    ) -> int:
        """Delete multiple DockerContainer records.

        Parameters
        ----------
        where
            Optional DockerContainer filter to find the records to be deleted

        Returns
        -------
        int
            The total number of DockerContainer records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all DockerContainer records
        total = await DockerContainer.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.DockerContainerScalarFieldKeys'],
        *,
        where: Optional['types.DockerContainerWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DockerContainerAvgAggregateInput'] = None,
        sum: Optional['types.DockerContainerSumAggregateInput'] = None,
        min: Optional['types.DockerContainerMinAggregateInput'] = None,
        max: Optional['types.DockerContainerMaxAggregateInput'] = None,
        having: Optional['types.DockerContainerScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DockerContainerCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DockerContainerScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DockerContainerScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DockerContainerGroupByOutput']:
        """Group DockerContainer records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar DockerContainer fields to group records by
        where
            DockerContainer filter to select records
        take
            Limit the maximum number of DockerContainer records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DockerContainerGroupByOutput]
            A list of dictionaries representing the DockerContainer record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group DockerContainer records by name values
        # and count how many records are in each group
        results = await DockerContainer.prisma().group_by(
            ['name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class LogEntryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LogEntry]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LogEntry.prisma().query_raw(
            'SELECT * FROM LogEntry WHERE id = $1',
            1675546029,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LogEntry
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LogEntry.prisma().query_first(
            'SELECT * FROM LogEntry WHERE containerId = $1',
            'bhghchehcc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LogEntryCreateInput,
        include: Optional[types.LogEntryInclude] = None
    ) -> _PrismaModelT:
        """Create a new LogEntry record.

        Parameters
        ----------
        data
            LogEntry record data
        include
            Specifies which relations should be loaded on the returned LogEntry model

        Returns
        -------
        prisma.models.LogEntry
            The created LogEntry record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LogEntry record from just the required fields
        logentry = await LogEntry.prisma().create(
            data={
                # data to create a LogEntry record
                'containerId': 'dcgchcbbf',
                'message': 'bdedcabahc',
                'level': 'ghfhiafcb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LogEntryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LogEntry records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LogEntry record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LogEntry.prisma().create_many(
            data=[
                {
                    # data to create a LogEntry record
                    'containerId': 'heejgedji',
                    'message': 'bjgjgibgbf',
                    'level': 'bbbgbhfjge',
                },
                {
                    # data to create a LogEntry record
                    'containerId': 'igbehcbab',
                    'message': 'bdadaadhag',
                    'level': 'bgiggdidbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LogEntryWhereUniqueInput,
        include: Optional[types.LogEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LogEntry record.

        Parameters
        ----------
        where
            LogEntry filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LogEntry model

        Returns
        -------
        prisma.models.LogEntry
            The deleted LogEntry record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        logentry = await LogEntry.prisma().delete(
            where={
                'id': 2000430152,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LogEntryWhereUniqueInput,
        include: Optional[types.LogEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LogEntry record.

        Parameters
        ----------
        where
            LogEntry filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LogEntry model

        Returns
        -------
        prisma.models.LogEntry
            The found LogEntry record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        logentry = await LogEntry.prisma().find_unique(
            where={
                'id': 1868141281,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LogEntryWhereUniqueInput,
        include: Optional[types.LogEntryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LogEntry record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LogEntry filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LogEntry model

        Returns
        -------
        prisma.models.LogEntry
            The found LogEntry record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        logentry = await LogEntry.prisma().find_unique_or_raise(
            where={
                'id': 1860847622,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogEntryWhereInput] = None,
        cursor: Optional[types.LogEntryWhereUniqueInput] = None,
        include: Optional[types.LogEntryInclude] = None,
        order: Optional[Union[types.LogEntryOrderByInput, List[types.LogEntryOrderByInput]]] = None,
        distinct: Optional[List[types.LogEntryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LogEntry records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LogEntry records returned
        skip
            Ignore the first N results
        where
            LogEntry filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LogEntry model
        order
            Order the returned LogEntry records by any field
        distinct
            Filter LogEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LogEntry]
            The list of all LogEntry records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LogEntry records
        logentrys = await LogEntry.prisma().find_many(take=10)

        # find the first 5 LogEntry records ordered by the message field
        logentrys = await LogEntry.prisma().find_many(
            take=5,
            order={
                'message': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LogEntryWhereInput] = None,
        cursor: Optional[types.LogEntryWhereUniqueInput] = None,
        include: Optional[types.LogEntryInclude] = None,
        order: Optional[Union[types.LogEntryOrderByInput, List[types.LogEntryOrderByInput]]] = None,
        distinct: Optional[List[types.LogEntryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LogEntry record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LogEntry filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LogEntry model
        order
            Order the returned LogEntry records by any field
        distinct
            Filter LogEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LogEntry
            The first LogEntry record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LogEntry record ordered by the level field
        logentry = await LogEntry.prisma().find_first(
            skip=1,
            order={
                'level': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LogEntryWhereInput] = None,
        cursor: Optional[types.LogEntryWhereUniqueInput] = None,
        include: Optional[types.LogEntryInclude] = None,
        order: Optional[Union[types.LogEntryOrderByInput, List[types.LogEntryOrderByInput]]] = None,
        distinct: Optional[List[types.LogEntryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LogEntry record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LogEntry filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LogEntry model
        order
            Order the returned LogEntry records by any field
        distinct
            Filter LogEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LogEntry
            The first LogEntry record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LogEntry record ordered by the timestamp field
        logentry = await LogEntry.prisma().find_first_or_raise(
            skip=1,
            order={
                'timestamp': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LogEntryUpdateInput,
        where: types.LogEntryWhereUniqueInput,
        include: Optional[types.LogEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LogEntry record.

        Parameters
        ----------
        data
            LogEntry record data specifying what to update
        where
            LogEntry filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LogEntry model

        Returns
        -------
        prisma.models.LogEntry
            The updated LogEntry record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        logentry = await LogEntry.prisma().update(
            where={
                'id': 1448521415,
            },
            data={
                # data to update the LogEntry record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LogEntryWhereUniqueInput,
        data: types.LogEntryUpsertInput,
        include: Optional[types.LogEntryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LogEntry filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LogEntry model

        Returns
        -------
        prisma.models.LogEntry
            The created or updated LogEntry record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        logentry = await LogEntry.prisma().upsert(
            where={
                'id': 1628650740,
            },
            data={
                'create': {
                    'id': 1628650740,
                    'containerId': 'igbehcbab',
                    'message': 'bdadaadhag',
                    'level': 'bgiggdidbf',
                },
                'update': {
                    'containerId': 'igbehcbab',
                    'message': 'bdadaadhag',
                    'level': 'bgiggdidbf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LogEntryUpdateManyMutationInput,
        where: types.LogEntryWhereInput,
    ) -> int:
        """Update multiple LogEntry records

        Parameters
        ----------
        data
            LogEntry data to update the selected LogEntry records to
        where
            Filter to select the LogEntry records to update

        Returns
        -------
        int
            The total number of LogEntry records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LogEntry records
        total = await LogEntry.prisma().update_many(
            data={
                'id': 1249606685
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogEntryWhereInput] = None,
        cursor: Optional[types.LogEntryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LogEntry records present in the database

        Parameters
        ----------
        select
            Select the LogEntry fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LogEntry filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LogEntryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LogEntry.prisma().count()

        # results: prisma.types.LogEntryCountAggregateOutput
        results = await LogEntry.prisma().count(
            select={
                '_all': True,
                'containerId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LogEntryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogEntryWhereInput] = None,
        cursor: Optional[types.LogEntryWhereUniqueInput] = None,
    ) -> types.LogEntryCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LogEntryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogEntryWhereInput] = None,
        cursor: Optional[types.LogEntryWhereUniqueInput] = None,
    ) -> Union[int, types.LogEntryCountAggregateOutput]:
        """Count the number of LogEntry records present in the database

        Parameters
        ----------
        select
            Select the LogEntry fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LogEntry filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LogEntryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LogEntry.prisma().count()

        # results: prisma.types.LogEntryCountAggregateOutput
        results = await LogEntry.prisma().count(
            select={
                '_all': True,
                'message': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LogEntryCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LogEntryWhereInput] = None
    ) -> int:
        """Delete multiple LogEntry records.

        Parameters
        ----------
        where
            Optional LogEntry filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LogEntry records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LogEntry records
        total = await LogEntry.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LogEntryScalarFieldKeys'],
        *,
        where: Optional['types.LogEntryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LogEntryAvgAggregateInput'] = None,
        sum: Optional['types.LogEntrySumAggregateInput'] = None,
        min: Optional['types.LogEntryMinAggregateInput'] = None,
        max: Optional['types.LogEntryMaxAggregateInput'] = None,
        having: Optional['types.LogEntryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LogEntryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LogEntryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LogEntryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LogEntryGroupByOutput']:
        """Group LogEntry records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LogEntry fields to group records by
        where
            LogEntry filter to select records
        take
            Limit the maximum number of LogEntry records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LogEntryGroupByOutput]
            A list of dictionaries representing the LogEntry record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LogEntry records by level values
        # and count how many records are in each group
        results = await LogEntry.prisma().group_by(
            ['level'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AlertActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Alert]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Alert.prisma().query_raw(
            'SELECT * FROM Alert WHERE id = $1',
            835903122,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Alert
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Alert.prisma().query_first(
            'SELECT * FROM Alert WHERE containerId = $1',
            'hgdhbjhhj',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AlertCreateInput,
        include: Optional[types.AlertInclude] = None
    ) -> _PrismaModelT:
        """Create a new Alert record.

        Parameters
        ----------
        data
            Alert record data
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The created Alert record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Alert record from just the required fields
        alert = await Alert.prisma().create(
            data={
                # data to create a Alert record
                'containerId': 'ecjjjfbae',
                'name': 'bhhfibbigf',
                'condition': 'ijdbeffgg',
                'threshold': 995405759.210273,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AlertCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Alert records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Alert record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Alert.prisma().create_many(
            data=[
                {
                    # data to create a Alert record
                    'containerId': 'chbfcacbd',
                    'name': 'efggddide',
                    'condition': 'caficfigfb',
                    'threshold': 1583689592.87844,
                },
                {
                    # data to create a Alert record
                    'containerId': 'bghfciaafe',
                    'name': 'bgchfhgceh',
                    'condition': 'cafeiaccbc',
                    'threshold': 60335757.68446,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Alert record.

        Parameters
        ----------
        where
            Alert filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The deleted Alert record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().delete(
            where={
                'id': 1625503827,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Alert record.

        Parameters
        ----------
        where
            Alert filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The found Alert record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().find_unique(
            where={
                'id': 521827728,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Alert record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Alert filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The found Alert record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().find_unique_or_raise(
            where={
                'id': 1266032265,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
        include: Optional[types.AlertInclude] = None,
        order: Optional[Union[types.AlertOrderByInput, List[types.AlertOrderByInput]]] = None,
        distinct: Optional[List[types.AlertScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Alert records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Alert records returned
        skip
            Ignore the first N results
        where
            Alert filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Alert model
        order
            Order the returned Alert records by any field
        distinct
            Filter Alert records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Alert]
            The list of all Alert records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Alert records
        alerts = await Alert.prisma().find_many(take=10)

        # find the first 5 Alert records ordered by the name field
        alerts = await Alert.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
        include: Optional[types.AlertInclude] = None,
        order: Optional[Union[types.AlertOrderByInput, List[types.AlertOrderByInput]]] = None,
        distinct: Optional[List[types.AlertScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Alert record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Alert filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Alert model
        order
            Order the returned Alert records by any field
        distinct
            Filter Alert records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Alert
            The first Alert record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Alert record ordered by the condition field
        alert = await Alert.prisma().find_first(
            skip=1,
            order={
                'condition': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
        include: Optional[types.AlertInclude] = None,
        order: Optional[Union[types.AlertOrderByInput, List[types.AlertOrderByInput]]] = None,
        distinct: Optional[List[types.AlertScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Alert record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Alert filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Alert model
        order
            Order the returned Alert records by any field
        distinct
            Filter Alert records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Alert
            The first Alert record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Alert record ordered by the threshold field
        alert = await Alert.prisma().find_first_or_raise(
            skip=1,
            order={
                'threshold': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AlertUpdateInput,
        where: types.AlertWhereUniqueInput,
        include: Optional[types.AlertInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Alert record.

        Parameters
        ----------
        data
            Alert record data specifying what to update
        where
            Alert filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The updated Alert record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        alert = await Alert.prisma().update(
            where={
                'id': 93253262,
            },
            data={
                # data to update the Alert record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AlertWhereUniqueInput,
        data: types.AlertUpsertInput,
        include: Optional[types.AlertInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Alert filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Alert model

        Returns
        -------
        prisma.models.Alert
            The created or updated Alert record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alert = await Alert.prisma().upsert(
            where={
                'id': 2053047983,
            },
            data={
                'create': {
                    'id': 2053047983,
                    'containerId': 'bghfciaafe',
                    'name': 'bgchfhgceh',
                    'condition': 'cafeiaccbc',
                    'threshold': 60335757.68446,
                },
                'update': {
                    'containerId': 'bghfciaafe',
                    'name': 'bgchfhgceh',
                    'condition': 'cafeiaccbc',
                    'threshold': 60335757.68446,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AlertUpdateManyMutationInput,
        where: types.AlertWhereInput,
    ) -> int:
        """Update multiple Alert records

        Parameters
        ----------
        data
            Alert data to update the selected Alert records to
        where
            Filter to select the Alert records to update

        Returns
        -------
        int
            The total number of Alert records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Alert records
        total = await Alert.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Alert records present in the database

        Parameters
        ----------
        select
            Select the Alert fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Alert filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AlertCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Alert.prisma().count()

        # results: prisma.types.AlertCountAggregateOutput
        results = await Alert.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AlertCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
    ) -> types.AlertCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AlertCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AlertWhereInput] = None,
        cursor: Optional[types.AlertWhereUniqueInput] = None,
    ) -> Union[int, types.AlertCountAggregateOutput]:
        """Count the number of Alert records present in the database

        Parameters
        ----------
        select
            Select the Alert fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Alert filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AlertCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Alert.prisma().count()

        # results: prisma.types.AlertCountAggregateOutput
        results = await Alert.prisma().count(
            select={
                '_all': True,
                'containerId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AlertCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AlertWhereInput] = None
    ) -> int:
        """Delete multiple Alert records.

        Parameters
        ----------
        where
            Optional Alert filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Alert records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Alert records
        total = await Alert.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AlertScalarFieldKeys'],
        *,
        where: Optional['types.AlertWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AlertAvgAggregateInput'] = None,
        sum: Optional['types.AlertSumAggregateInput'] = None,
        min: Optional['types.AlertMinAggregateInput'] = None,
        max: Optional['types.AlertMaxAggregateInput'] = None,
        having: Optional['types.AlertScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AlertCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AlertScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AlertScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AlertGroupByOutput']:
        """Group Alert records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Alert fields to group records by
        where
            Alert filter to select records
        take
            Limit the maximum number of Alert records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AlertGroupByOutput]
            A list of dictionaries representing the Alert record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Alert records by name values
        # and count how many records are in each group
        results = await Alert.prisma().group_by(
            ['name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models